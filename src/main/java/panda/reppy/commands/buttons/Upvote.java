package panda.reppy.commands.buttons;

import net.dv8tion.jda.api.events.interaction.ButtonClickEvent;

public class Upvote {

    public static void process(ButtonClickEvent event) {
//        if (!actorId.equals(authorId)) {
//            ModelDao.generateVoteModel(event.getChannel().getId(), answerModel.getPostId(), actorId,
//                    authorId, true);
//        }
//
//        Guild guild = event.getGuild();
//        if (guild != null) {
//            Member author = guild.getMemberById(authorId);
//
//            MessageEmbed oldEmbed = message.getEmbeds().get(0);
//            String content = oldEmbed.getFields().get(0).getValue();
//
//            MessageEmbed updatedEmbed = generateAnswerEmbed(author, content, message.getId(), true);
//            message.editMessageEmbeds(updatedEmbed).queue();
//        }
    }
}
